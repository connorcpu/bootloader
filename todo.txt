restructure
setup/generate standard library for cross-compliler
complete filesystem 
   - directories
   - writing to files
terminal
propperly setup IO
   - propper filedescriptors
   - sooooo we need a file object which stores a couple things
   - then filedescriptors point to file objects
   - where the file object is a struct which stores some info, like the lseek offset
   - directories should just be a type of file
   - ideally you don't busy loop but instead you put the thread to sleep and wake it upon the fd becoming ready
   - any io devices should trigger an interrupt, which should put data into a buffer
   - then upon read() being called this buffer is 'consumed'

no current problems

next up: more syscalls -> setup to terminal, or PCI or better memory management

   - BRUH, syscalls never save any form of register (or atleast restore) and we are wondering WHY IT CLOBERS STUFF???? AM I STUPID
   - baponkar.github.io/Osdev-Notes/
   - switch to sysret at some point
   - syscalls to implement:
      - read (to file/fd)
      - write (to file/fd)
      - open 
      - close
      - lseek (allows writing at offsets which we need for the framebuffer)
      - mmap (probably usefull, also for doing framebuffer manipulation)
      - poll (think i need it or select() for user input)
         - implemented a small thing to use with keyboard input
      - {,f,l}stat (gets some stats on fd's)
         - we want fstat since it used filedescriptors instead of paths
      - ioctl (maybe, is used to manipulate fd's)
      - far future: socket calls like bind connect accept etc
   - we miiiiight wanna do multithreading:
      - you need to sent "INIT IPI" & "SIPI" over the APIC to wake a thread
      - then you need to trampoline the core out of 16-bit mode much like the first thread
         - this need physical 0x8000 memory....
      - DO NOT REUSE STACK
   - we mighhhttt wanna do some PCI stuff, seems doable
   - look at SYSV ABI

   - memory map is in higher, half
   - loadfile(char* filename) is a thing
   - loadElf(char* filename) is a thing
   - stack is higher half

- objdump
   - -d / -D to disassembly
   - -S to show source code

- hexdump 
   - -C to translate to ascii

- bochs
   - x for virtual memory
   - xp for physical memory
      - /16w for 16 words
      - /16wc to get 16 words in ascii

- inline asm
   - volatile __asm__(instructions : output operands : input operands : clobbered registers);
   - use %% for registers and %0/%1 for C variables
   - when outputting, a for eax, b for ebx etc
   - when outputting, use '"=a" (EAX)' to output to EAX, the = means don't care about EAX initial value
   - gcc can figure out that if an register is used for output it gets clobbered
   - use "r" as constraint when you want the var to go throuh some register, lets compiler choose
   - use "g" if you really don't give a shit

- qemu PCI devs: 
   - intel, bridge:host bridge
   - intel, bridge:ISA bridge (header type 0x80 means multifuncional)
      - intel, mass storage controller:IDE controller, ISA Compatiblilyt mode-only controller, supports bus mastering
   - bogus, VGA-compatible controller:VGA controller
   - intel, network controller:Ethernet controller
